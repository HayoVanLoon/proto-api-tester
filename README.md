# Protocol Buffer API Tester

Uses the descriptor set generated by protoc to generate a frontend for services 
contained.

Early work in progress.

```shell
# build json en/decoders
flutter pub run build_runner build
```